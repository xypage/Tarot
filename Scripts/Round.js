class Round {
	constructor(_players, firstPlayer, cardsLeft) {
		this.players = _players;
		this.currentPlayer = firstPlayer;
		this.lastRound = cardsLeft == 1;
	}

	play() {
		//loop
			//calls currentPlayer.play

			//goes to next player
		//whoever played winning card gets hand
	}
	
}